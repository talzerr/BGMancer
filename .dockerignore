node_modules
.next

# Environment files (injected at runtime via docker-compose env_file)
.env
.env.local
.env.docker

# Local data — persisted via Docker volumes instead
bgmancer.db
snapshots/

# Dev/CI artifacts
*.log
.git
.gitignore
