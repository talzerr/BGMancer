# Test environment — placeholder values for CI and local test runs.
# No real API calls are made. Committed to repo (no secrets).

NEXTAUTH_SECRET=test-secret-not-real
NEXTAUTH_URL=http://localhost:6959
YOUTUBE_API_KEY=test-placeholder
STEAM_API_KEY=test-placeholder
ANTHROPIC_API_KEY=test-placeholder
